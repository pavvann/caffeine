node_modules
out
dist
.DS_Store
*.log
.env
.env.local
.caffeine-pipeline-complete

# Caffeine runtime artifacts — generated during agent runs, not source.
# Track these in your own project repos if you want; they don't belong
# in the Caffeine repo itself.
BACKLOG.md
STATE.md
pipeline.md

# Generated icon artifacts — derived from build/icon.png by
# scripts/generate-icns.cjs. The .png is the source of truth and is
# tracked; everything below regenerates on any macOS install via
# `pnpm generate-icns` (also runs on postinstall).
build/icon.iconset/
build/icon.icns
build/icon-mac.png
